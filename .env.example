# ─────────────────────────────────────────────────────────────────────────────
# Google Cloud / Vertex AI
# ─────────────────────────────────────────────────────────────────────────────
# GCP project ID
GOOGLE_CLOUD_PROJECT=

# Vertex AI region — us-east5 supports Claude Sonnet on Vertex
GOOGLE_CLOUD_LOCATION=us-east5

# Path to service account JSON key file
# Required IAM roles:
#   roles/aiplatform.user      — Vertex AI model inference
#   roles/calendar.events      — Google Calendar read/write
GOOGLE_APPLICATION_CREDENTIALS=

# ─────────────────────────────────────────────────────────────────────────────
# LLM Provider (choose one)
# ─────────────────────────────────────────────────────────────────────────────
# Option A: Anthropic API (recommended — simpler, no GCP required)
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Option B: Vertex AI (leave ANTHROPIC_API_KEY blank to use this path)
# Requires GOOGLE_CLOUD_PROJECT, GOOGLE_CLOUD_LOCATION, and GOOGLE_APPLICATION_CREDENTIALS below

# ─────────────────────────────────────────────────────────────────────────────
# Lead Enrichment
# ─────────────────────────────────────────────────────────────────────────────
# Exa semantic search — https://exa.ai
EXA_API_KEY=

# Hunter.io email finder — https://hunter.io (cheaper than Apollo)
HUNTER_API_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# Hunter.io Email Sequences (optional — email sending via Hunter Campaigns)
# ─────────────────────────────────────────────────────────────────────────────
# Create your sequence in the Hunter UI first, then paste its ID here.
# Find the ID: Hunter Dashboard → Campaigns → click sequence → ID in URL
HUNTER_CAMPAIGN_ID=
# Set to true to auto-start the sequence after adding recipients.
# WARNING: Emails will send immediately. Leave false for manual review.
HUNTER_AUTO_START=false

# ─────────────────────────────────────────────────────────────────────────────
# Voice — ElevenLabs
# ─────────────────────────────────────────────────────────────────────────────
# ElevenLabs API key — https://elevenlabs.io
ELEVENLABS_API_KEY=

# ElevenLabs voice ID to use for the calling agent
# Clone Declan's voice or choose a professional voice from the library
ELEVENLABS_VOICE_ID=

# Set after the pipeline creates the Conversational AI agent on first run
ELEVENLABS_CONV_AGENT_ID=

# ─────────────────────────────────────────────────────────────────────────────
# Google Calendar
# ─────────────────────────────────────────────────────────────────────────────
# Declan's Google Calendar ID (usually his email address)
DECLAN_CALENDAR_ID=

# ─────────────────────────────────────────────────────────────────────────────
# Email Sending
# ─────────────────────────────────────────────────────────────────────────────
# The email address outreach is sent from
SENDER_EMAIL=

# Display name for outbound emails
SENDER_NAME=Backflip Media

# ─────────────────────────────────────────────────────────────────────────────
# Notes
# ─────────────────────────────────────────────────────────────────────────────
# Claude is accessed via Anthropic API (ANTHROPIC_API_KEY) by default.
# Leave ANTHROPIC_API_KEY blank to fall back to Vertex AI (billed to your GCP project).

# ─────────────────────────────────────────────────────────────────────────────
# Database (PostgreSQL via Docker Compose)
# ─────────────────────────────────────────────────────────────────────────────
# Password for the 'backflip' PostgreSQL user
DB_PASSWORD=<your-db-password-here>

# NOTE: The password in DATABASE_URL must match DB_PASSWORD above.
# Full async DB URL (used by SQLAlchemy)
# NOTE: If you have a local PostgreSQL installation already running on port 5432,
# change the host port in docker-compose.yml to "5433:5432" and update this URL to use port 5433.
# Alternatively, use the Docker container's OrbStack IP directly (check: docker inspect <container>).
DATABASE_URL=postgresql+asyncpg://backflip:<your-db-password-here>@localhost:5432/backflip_sdr

# pgAdmin credentials (web UI at http://localhost:5050)
PGADMIN_EMAIL=admin@backflip.dev
PGADMIN_PASSWORD=<your-pgadmin-password-here>

# Connection pool settings (tune per environment)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# ─────────────────────────────────────────────────────────────────────────────
# Observability — Langfuse Cloud (https://cloud.langfuse.com)
# ─────────────────────────────────────────────────────────────────────────────
LANGFUSE_PUBLIC_KEY=pk-lf-
LANGFUSE_SECRET_KEY=sk-lf-
LANGFUSE_HOST=https://cloud.langfuse.com
